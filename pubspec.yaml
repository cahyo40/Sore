name: sore_book
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  calendar_timeline: ^1.1.3
  flutter:
    sdk: flutter
  get: ^4.6.6
  google_fonts: ^6.2.1
  icons_plus: ^5.0.0
  intl: ^0.19.0
  loading_animation_widget: ^1.2.1
  yo_package:
    path: ../package/yo_package

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    # - assets/icons/
